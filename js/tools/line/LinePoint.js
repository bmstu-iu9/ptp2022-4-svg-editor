class LinePoint extends Point {
	constructor(figure, cx, cy) {
		super(figure, cx, cy);
		this.enableLineTransformation();
	}

	enableLineTransformation() {
		//TODO: реализовать гомотетию, поворот.
	}
}